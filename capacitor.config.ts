import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lunarcapybaraspin',
  appName: 'lunar-capybara-spin',
  webDir: 'dist'
};

export default config;
